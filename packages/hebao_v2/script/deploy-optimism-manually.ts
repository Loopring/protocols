const hre = require("hardhat");
const ethers = hre.ethers;

async function flattenAll() {}

async function main() {}

main()
  .then(() => process.exit(0))
  .catch(err => {
    console.error(err);
    process.exit(1);
  });
