dist: trusty
sudo: false

language: node_js
node_js:
  - "8"

install:
  - npm install
  - npm install -g ganache-cli

before_script:
  - ganache-cli > ganache_cli.log &

script:
  - npm run build
  
cache:
  directories:
    - node_modules
